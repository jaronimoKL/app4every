:80 {
    # Для простоты локального запуска без DNS-имен проксируем обычный HTTP порт 80
    
    # API запросы на Go Backend
    handle /api/* {
        reverse_proxy auth-service:8080
    }

    # Все остальные запросы на Vue dev server
    handle {
        reverse_proxy frontend:5173
    }
}
